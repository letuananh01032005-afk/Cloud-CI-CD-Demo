/* --- CSS GIAO DIỆN CHỦ ĐỀ --- */
:root {
    --bg-gradient: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
    --card-bg: rgba(255, 255, 255, 0.1);
    --text-color: #ffffff;
    --btn-bg: #00f2fe;
    --btn-text: #000000;
    --shadow: rgba(0, 0, 0, 0.3);
}

/* Khi đổi sang chủ đề Sáng (Light Mode) */
[data-theme="light"] {
    --bg-gradient: linear-gradient(135deg, #e0c3fc, #8ec5fc);
    --card-bg: rgba(255, 255, 255, 0.6);
    --text-color: #2d3748;
    --btn-bg: #4f46e5;
    --btn-text: #ffffff;
    --shadow: rgba(0, 0, 0, 0.1);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    min-height: 100vh;
    background: var(--bg-gradient);
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background 0.5s ease;
    padding: 20px;
}

/* --- THIẾT KẾ CARD HIỆU ỨNG KÍNH MỜ --- */
.container {
    background: var(--card-bg);
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 40px;
    border-radius: 20px;
    width: 100%;
    max-width: 450px;
    text-align: center;
    box-shadow: 0 8px 32px 0 var(--shadow);
    color: var(--text-color);
    transition: all 0.3s ease;
}

.container:hover {
    transform: translateY(-5px);
}

h1 {
    font-size: 1.8rem;
    margin-bottom: 10px;
    font-weight: 700;
}

.subtitle {
    font-size: 0.95rem;
    opacity: 0.8;
    margin-bottom: 30px;
}

/* Khu vực hiển thị thông điệp tương tác */
.interactive-box {
    background: rgba(0, 0, 0, 0.1);
    border-radius: 12px;
    padding: 20px;
    margin-bottom: 25px;
    min-height: 100px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px dashed rgba(255, 255, 255, 0.3);
}

#message {
    font-size: 1.1rem;
    font-style: italic;
    line-height: 1.5;
}

/* Nút bấm tương tác */
.btn {
    background: var(--btn-bg);
    color: var(--btn-text);
    border: none;
    padding: 12px 28px;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 50px;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    transition: all 0.3s ease;
    width: 100%;
    margin-bottom: 15px;
}

.btn:hover {
    opacity: 0.9;
    transform: scale(1.02);
}

.btn:active {
    transform: scale(0.98);
}

/* Nút đổi giao diện (Theme) */
.theme-toggle {
    background: transparent;
    border: 1px solid var(--text-color);
    color: var(--text-color);
    padding: 8px 16px;
    font-size: 0.85rem;
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.theme-toggle:hover {
    background: var(--text-color);
    color: #000;
}
